extends layout

block content
  .main.container.text-xs-center
    h1.display-3 Voting app
    p.lead Make your own polls!
    if !currentUser
      a.btn.btn-info-outline.btn-lg(href='/register') Get Started for FREE
    if currentUser
      a.btn.btn-info-outline.btn-lg(href='/polls_list') Look at the POLLS


