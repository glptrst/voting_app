extends layout

block content
  .main.container.clearfix

      p

      h1 My Polls

      if !myPolls.length
        p you haven't created any poll so far.
      else
        ul
          each val in myPolls
            a(href="/poll?title=" + val)
              li= val
