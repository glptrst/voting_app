extends layout

block content
  .main.container.text-xs-center
      h1.display-3 Polls

      if !polls.length
        p There are no polls :O
      ul
        each val in polls
          p
            a(href="/poll?title=" + val.title) #{val.title}
